# API Configuration
# The API base URL for backend services
API_BASE_URL=https://api.echoreads.online/api/v1
NEXT_PUBLIC_API_BASE_URL=https://api.echoreads.online/api/v1

# Demo Mode (only for development)
# Set to 'true' to enable demo authentication fallback when API is unavailable
# WARNING: Only use in development! Never enable in production.
ENABLE_DEMO_MODE=false

# Next.js Configuration
# The public URL of your application
NEXT_PUBLIC_APP_URL=http://localhost:3000
